import { StatusMessageTransform } from './status-message-transform';

describe('StatusMessageTransform', () => {
  it('should create an instance', () => {
    expect(new StatusMessageTransform()).toBeTruthy();
  });
});
